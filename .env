# SQLite database — swap DATABASE_URL to a postgres:// URL to migrate to Postgres
DATABASE_URL="file:./dev.db"

# Session signing secret — CHANGE THIS in production (openssl rand -base64 32)
AUTH_SECRET="dev-secret-change-me-in-production-0a1b2c3d4e5f6g7h"

# Public base URL used in client-facing intake links
NEXT_PUBLIC_APP_URL="http://localhost:3000"
